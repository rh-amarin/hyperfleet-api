# Default values for hyperfleet-api
# This is a YAML-formatted file.

# -- Number of API server replicas
replicaCount: 1

image:
  # -- Container image registry (no default — must be set)
  registry: CHANGE_ME
  # -- Container image repository (no default — must be set)
  repository: CHANGE_ME
  # -- Image pull policy
  pullPolicy: Always
  # -- Image tag (no default — must be set via `--set image.tag=<version>`)
  tag: ""

# -- Secrets for pulling images from private registries
imagePullSecrets: []
# -- Override the chart name used in resource names
nameOverride: ""
# -- Override the full release name used in resource names
fullnameOverride: ""

# -- Container ports exposed by the API server. These must match the
# corresponding application config values (`config.server.port`,
# `config.health.port`, `config.metrics.port`).
ports:
  # -- API server port
  api: 8000
  # -- Health check endpoint port
  health: 8080
  # -- Prometheus metrics endpoint port
  metrics: 9090

# -- Application configuration. All settings in this section generate the
# ConfigMap consumed by the API server. Set `config.existingConfigMap` to
# use a pre-existing ConfigMap instead.
config:
  # -- Use an existing ConfigMap instead of generating one. When set, all
  # other `config.*` values are ignored.
  existingConfigMap: ""

  # -- HTTP server settings
  server:
    # -- Public hostname advertised by the API (leave empty for auto-detect)
    hostname: ""
    # -- Listen address
    host: "0.0.0.0"
    # -- Listen port (must match `ports.api`)
    port: 8000

    # -- Request timeout settings
    timeouts:
      # -- HTTP read timeout
      read: 5s
      # -- HTTP write timeout
      write: 30s

    # -- TLS configuration for the API server
    tls:
      # -- Enable TLS on the API listener
      enabled: false
      # -- Path to TLS certificate file
      cert_file: ""
      # -- Path to TLS key file
      key_file: ""

    # -- JWT authentication settings
    jwt:
      # -- Enable JWT authentication
      enabled: false
      # -- List of JWT issuer configurations (multi-issuer support)
      configs: []
      # Example:
      # configs:
      #   - issuer_url: "https://idp.example.com/auth/realms/my-realm"
      #     jwk_cert_url: "https://idp.example.com/certs"
      #     jwk_cert_ca_file: ""
      #     audience: "my-api"
      #     identity_claim: "email"
      #     identity_claim_pattern: "^[^@]+@example\\.com$"
      #     identity_header: "X-HyperFleet-Identity"
      #   - issuer_url: "https://other-idp.example.com"
      #     jwk_cert_file: "/etc/hyperfleet/jwks.json"
      #     header: "X-Custom-Auth"

  # -- Database connection settings. Credentials must be provided via a
  # Secret — see `database.external.secretName` or use the built-in
  # PostgreSQL (`database.postgresql.enabled`).
  database:
    # -- SQL dialect
    dialect: postgres

    # -- Database host (auto-set when using the built-in PostgreSQL)
    host: ""
    # -- Database port
    port: 5432
    # -- Database name
    name: hyperfleet

    # -- Enable SQL debug logging
    debug: false

    # -- SSL / TLS settings for the database connection
    ssl:
      # -- SSL mode (`disable`, `require`, `verify-ca`, `verify-full`)
      mode: disable
      # -- Path to the CA root certificate
      root_cert_file: ""

    # -- Connection pool tuning
    pool:
      # -- Maximum number of open connections
      max_connections: 50
      # -- Maximum number of idle connections
      max_idle_connections: 10
      # -- Maximum lifetime of a connection
      conn_max_lifetime: 5m
      # -- Maximum idle time before a connection is closed
      conn_max_idle_time: 1m
      # -- Timeout for acquiring a connection from the pool
      request_timeout: 30s
      # -- Number of connection retry attempts on startup
      conn_retry_attempts: 10
      # -- Interval between connection retry attempts
      conn_retry_interval: 3s

  # -- Logging configuration
  logging:
    # -- Log level (`debug`, `info`, `warn`, `error`)
    level: info
    # -- Log format (`json` or `text`)
    format: json
    # -- Log output destination
    output: stdout

    # -- OpenTelemetry tracing integration.
    # See the [tracing standard](https://github.com/openshift-hyperfleet/architecture/blob/main/hyperfleet/standards/tracing.md#configuration).
    otel:
      # -- Enable OpenTelemetry log correlation
      enabled: false

    # -- Sensitive-data masking for logs
    masking:
      # -- Enable log masking
      enabled: true
      # -- HTTP headers whose values are redacted in logs
      headers:
        - Authorization
        - X-API-Key
        - Cookie
        - X-Auth-Token
        - X-Forwarded-Authorization
        - X-HyperFleet-Identity
      # -- Field names whose values are redacted in logs
      fields:
        - password
        - secret
        - token
        - api_key
        - access_token
        - refresh_token
        - client_secret

  # -- Prometheus metrics endpoint settings
  metrics:
    # -- Listen address (must be `0.0.0.0` for in-cluster access)
    host: 0.0.0.0
    # -- Listen port (must match `ports.metrics`)
    port: 9090

    # -- TLS configuration for the metrics endpoint
    tls:
      # -- Enable TLS on the metrics endpoint
      enabled: false

    # -- Duration window for label-based metric inclusion
    label_metrics_inclusion_duration: 168h
    # -- Threshold after which a pending reconciliation is considered stuck
    reconciliation_stuck_threshold: 10m

  # -- Health check endpoint settings
  health:
    # -- Listen address (must be `0.0.0.0` for probe access)
    host: 0.0.0.0
    # -- Listen port (must match `ports.health`)
    port: 8080

    # -- TLS configuration for the health endpoint
    tls:
      # -- Enable TLS on the health endpoint
      enabled: false

    # -- Graceful shutdown timeout
    shutdown_timeout: 20s
    # -- Timeout for the database liveness ping
    db_ping_timeout: 2s
  # -- Background reconciler (replaces hyperfleet-sentinel + broker).
  reconciler:
    # -- Enable the background reconciler goroutine.
    enabled: true
    # -- How often the reconciler scans all resources.
    poll_interval: 30s
    # -- Maximum time since last successful reconciliation before re-triggering adapters.
    stale_threshold: 30m

  # -- Entity descriptors registered at startup. Each entry auto-generates
  # REST endpoints, spec validation, and delete policies.
  entities:
    - kind: Cluster
      plural: clusters
      spec_schema_name: ClusterSpec
      required_adapters:
        validation: "http://hyperfleet-adapter-validation.hyperfleet.svc.cluster.local"
        dns: "http://hyperfleet-adapter-dns.hyperfleet.svc.cluster.local"
        pullsecret: "http://hyperfleet-adapter-pullsecret.hyperfleet.svc.cluster.local"
        hypershift: "http://hyperfleet-adapter-hypershift.hyperfleet.svc.cluster.local"

      name_min_len: 3
      name_max_len: 53
      require_spec_schema: true
    - kind: NodePool
      plural: nodepools
      parent_kind: Cluster
      on_parent_delete: cascade
      spec_schema_name: NodePoolSpec
      required_adapters:
        validation: "http://hyperfleet-adapter-validation.hyperfleet.svc.cluster.local"
        hypershift: "http://hyperfleet-adapter-hypershift.hyperfleet.svc.cluster.local"

      name_min_len: 3
      name_max_len: 15
      require_spec_schema: true
    - kind: Channel
      plural: channels
      spec_schema_name: ChannelSpec

    - kind: Version
      plural: versions
      parent_kind: Channel
      on_parent_delete: restrict
      spec_schema_name: VersionSpec

    - kind: WifConfig
      plural: wifconfigs
      spec_schema_name: WifConfigSpec


# -- ServiceAccount configuration
serviceAccount:
  # -- Create a ServiceAccount for the API server
  create: true
  # -- Annotations added to the ServiceAccount (e.g. for Workload Identity)
  annotations: {}
  # -- Override the ServiceAccount name (defaults to the release fullname)
  name: ""

# -- Additional annotations applied to all pods
podAnnotations: {}
# -- Additional labels applied to all pods
podLabels: {}

# -- Custom labels applied to all resources managed by this Helm chart
# (Deployment, Service, ServiceAccount, ConfigMap, PostgreSQL StatefulSet, etc.)
# Useful for labeling resources for cleanup, tracking, or organization.
# Example:
#   labels:
#     environment: production
#     team: platform
#     hyperfleet-e2e-run: e2e-20260615-102422-bb5nfo2d
# labels: {}

# -- Pod-level security context
podSecurityContext:
  # -- Filesystem group for volume mounts
  fsGroup: 65532
  # -- Run all containers as non-root
  runAsNonRoot: true
  # -- UID for all containers
  runAsUser: 65532

# -- Container-level security context
securityContext:
  # -- Disallow privilege escalation
  allowPrivilegeEscalation: false
  capabilities:
    drop:
    - ALL
  # -- Mount root filesystem as read-only
  readOnlyRootFilesystem: true
  seccompProfile:
    type: RuntimeDefault

# -- Kubernetes Service configuration
service:
  # -- Service type (`ClusterIP`, `LoadBalancer`, `NodePort`)
  type: ClusterIP

# -- CPU and memory resource requests and limits
resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 100m
    memory: 128Mi

# -- Container lifecycle hooks. Use `preStop` to delay SIGTERM during
# rolling updates, giving the LoadBalancer time to drain the old pod.
# See HYPERFLEET-1306.
lifecycle:
  preStop:
    exec:
      command:
        - /bin/sh
        - -c
        - sleep 5

# -- Deployment rollout strategy. `maxUnavailable: 0` ensures zero-downtime
# during rolling updates — the old pod stays until the new one is Ready.
strategy:
  rollingUpdate:
    maxSurge: 1
    maxUnavailable: 0
  type: RollingUpdate

# -- Seconds Kubernetes waits after SIGTERM before SIGKILL.
# Shutdown sequence is sequential: preStop sleep (5s) + health server (20s max)
# + API server (10s max) + metrics server (10s max) + OTel (20s max) + DB close
# (no timeout). Typical shutdown is ~8s total; worst case exceeds 60s.
# Set to 70s to cover worst case with buffer. If the DB close hangs beyond
# this, Kubernetes sends SIGKILL — acceptable as a last resort.
terminationGracePeriodSeconds: 70

# -- Node selector constraints for pod scheduling
nodeSelector: {}

# -- Tolerations for pod scheduling
tolerations: []

# -- Affinity rules for pod scheduling
affinity: {}

# -- Horizontal Pod Autoscaler configuration
autoscaling:
  # -- Enable the HPA
  enabled: false
  # -- Minimum number of replicas
  minReplicas: 1
  # -- Maximum number of replicas
  maxReplicas: 10
  # -- Target CPU utilization percentage
  targetCPUUtilizationPercentage: 80
  # -- Target memory utilization percentage
  targetMemoryUtilizationPercentage: 80

# -- PodDisruptionBudget configuration
podDisruptionBudget:
  # -- Enable the PDB
  enabled: false
  # -- Minimum number of available pods during disruption
  minAvailable: 1
  # maxUnavailable: 1

# -- Database infrastructure settings. For **production**, set
# `database.external.enabled=true` and supply a secret with connection
# details. For **development**, the built-in PostgreSQL pod is enabled
# by default.
database:
  # -- External database configuration (production)
  external:
    # -- Use an external database instead of the built-in PostgreSQL
    enabled: false
    # -- Name of an existing Secret with keys: `db.host`, `db.port`,
    # `db.name`, `db.user`, `db.password`
    secretName: ""

  # -- Built-in PostgreSQL for development and testing
  postgresql:
    # -- Deploy a single-pod PostgreSQL instance
    enabled: true
    # -- PostgreSQL container image
    image: docker.io/library/postgres:14.23
    # -- Database name
    database: hyperfleet
    # -- Database user
    user: hyperfleet
    # -- Database password (**development only** — use a Secret in production)
    password: hyperfleet-dev-password
    # -- PostgreSQL listen port
    port: 5432
    # -- Resource requests and limits for the PostgreSQL pod
    resources:
      limits:
        cpu: 500m
        memory: 512Mi
      requests:
        cpu: 100m
        memory: 128Mi
    # -- Persistent volume configuration for PostgreSQL data
    persistence:
      # -- Enable persistent storage (uses emptyDir when disabled)
      enabled: false
      # -- Volume size
      size: 1Gi
      # -- StorageClass name (empty for cluster default)
      storageClass: ""

# -- Monitoring and alerting configuration
monitoring:
  # -- PodMonitoring for Google Managed Prometheus (GMP) scraping
  podMonitoring:
    # -- Create a PodMonitoring resource
    enabled: false
    # -- Scrape interval
    interval: 30s
    # -- Additional labels for the PodMonitoring resource
    additionalLabels: {}
    # -- Metric relabel configs to apply to samples before ingestion
    metricRelabeling: []
    # -- TLS configuration when config.metrics.tls.enabled=true
    tlsConfig:
      # -- Disable target certificate validation (e.g. for self-signed certs)
      insecureSkipVerify: false
  # -- PrometheusRule for alerting
  prometheusRule:
    # -- Create PrometheusRule resources
    enabled: false
    # -- Additional labels for PrometheusRule discovery
    additionalLabels: {}
    # -- Namespace to create the PrometheusRule in (defaults to release namespace)
    namespace: ""
    # -- Alert rule configuration
    rules:
      # -- Alert when reconciliation is stuck
      reconciliationStuck:
        # -- Duration before the alert fires
        for: "5m"
        # -- Runbook URL included in the alert
        runbookUrl: ""
      # -- Alert when reconciliation exceeds timeout (based on actual stuck duration, survives Prometheus restarts)
      reconciliationTimeout:
        # -- Stuck duration in seconds that triggers the critical alert
        durationSeconds: 1800
        # -- Stabilization window before firing (short — the duration check is the real gate)
        for: "5m"
        # -- Runbook URL included in the alert
        runbookUrl: ""

# -- ServiceMonitor for Prometheus Operator scrape configuration
serviceMonitor:
  # -- Create a ServiceMonitor resource
  enabled: false
  # -- Scrape interval
  interval: 30s
  # -- Scrape timeout
  scrapeTimeout: 10s
  # -- Additional labels for ServiceMonitor discovery
  labels: {}
  # -- Namespace to create the ServiceMonitor in (defaults to release namespace)
  namespace: ""

# -- Distributed tracing configuration (OpenTelemetry)
tracing:
  # -- Enable trace export
  enabled: false
  # -- Service name reported in traces
  serviceName: "hyperfleet-api"
  # -- OTLP exporter endpoint (traces go to stdout when empty)
  otlpEndpoint: ""
  # -- OTLP protocol (`grpc` or `http/protobuf`)
  otlpProtocol: "grpc"
  # -- Sampler type
  sampler: "parentbased_traceidratio"
  # -- Sampling rate (`1.0` for dev, `0.01` for production)
  samplerArg: "1.0"
  # -- Context propagation formats
  propagators: "tracecontext,baggage"

# -- Native sidecar containers (Kubernetes 1.28+). Native sidecars are init
# containers with `restartPolicy: Always` — they start before other init
# containers and keep running throughout the pod lifecycle. Use this for
# database proxies that must be available during `db-migrate`.
# Each entry is a full Kubernetes container spec.
# @default -- `[]`
nativeSidecars: []
# Example: Cloud SQL Auth Proxy as native sidecar
#   - name: cloud-sql-proxy
#     restartPolicy: Always
#     image: gcr.io/cloud-sql-connectors/cloud-sql-proxy:2.14.3
#     args:
#       - "--auto-iam-authn"
#       - "--structured-logs"
#       - "--port=5432"
#       - "PROJECT:REGION:INSTANCE"
#     securityContext:
#       allowPrivilegeEscalation: false
#       capabilities:
#         drop: [ALL]
#       readOnlyRootFilesystem: true
#       runAsNonRoot: true
#       seccompProfile:
#         type: RuntimeDefault
#     resources:
#       requests:
#         cpu: 100m
#         memory: 64Mi
#       limits:
#         cpu: 200m
#         memory: 128Mi

# -- Regular sidecar containers. These start after init containers complete.
# Use `nativeSidecars` above for containers that must be available during
# init (e.g. database proxies). Each entry is a full Kubernetes container
# spec.
# @default -- `[]`
sidecars: []
# Example: PgBouncer connection pooler
#   - name: pgbouncer
#     image: public.ecr.aws/bitnami/pgbouncer:1.25.1
#     securityContext:
#       allowPrivilegeEscalation: false
#       capabilities:
#         drop: [ALL]
#       readOnlyRootFilesystem: true
#       seccompProfile:
#         type: RuntimeDefault
#     ports:
#       - name: pgbouncer
#         containerPort: 6432
#         protocol: TCP
#     env:
#       - name: POSTGRESQL_HOST
#         value: my-postgresql-host
#       - name: POSTGRESQL_PORT
#         value: "5432"
#       - name: POSTGRESQL_DATABASE
#         value: hyperfleet
#       - name: POSTGRESQL_USERNAME
#         value: hyperfleet
#       - name: POSTGRESQL_PASSWORD
#         valueFrom:
#           secretKeyRef:
#             name: my-db-secret
#             key: db.password
#       - name: PGBOUNCER_PORT
#         value: "6432"
#       - name: PGBOUNCER_POOL_MODE
#         value: transaction
#     resources:
#       limits:
#         cpu: 200m
#         memory: 128Mi
#       requests:
#         cpu: 50m
#         memory: 64Mi

# -- Validation schema configuration. Supply a custom OpenAPI schema for
# resource spec validation. When enabled, the schema is mounted into
# the container and every create/update request is validated against it. The
# API will fail to start if the schema is invalid.
validationSchema:
  # -- Enable spec validation
  enabled: false
  # -- Use an existing ConfigMap (must contain an `openapi.yaml` key).
  # When set, `validationSchema.content` is ignored.
  existingConfigMap: ""
  # -- Inline OpenAPI 3.0 schema content. Must define spec schemas matching
  # `spec_schema_name` for each entity (e.g. `ClusterSpec`, `NodePoolSpec`) under `components.schemas`.
  # @default -- `""`
  content: ""

# -- Additional environment variables injected into the API container.
# Use sparingly — prefer `config.*` values above.
extraEnv: []

# -- Extra volume mounts added to the API container
extraVolumeMounts: []

# -- Extra volumes added to the pod
extraVolumes: []
